    • Method to convert size_t to string like "41.6 Gi"
    • When copying or moving to removable media, sync mut be used.
    • Editing and viewing should use different Frame classes.
    • Viewer as image mode.
    • Viewer as hex mode.
    • Distinct appearance for sudo mode.
    • Control + F10 - all
    • Control + F11 - executable
    • Control + F12 - with mask
    • Visibility filtering by folders/hidden/other.
    • Test file save operations in the editor.
    • Editor must handle encrypted files
    • “save / discard / cancel” dialog when exiting the editor with unsaved changes.
    • F4 action on folders – show empty editor
    • F3 action on folders – should display total size in bytes.
    • Optional status line.
    • Configuration dialog bound to Ctrl+D.
    • File search (Alt+F7) - case sensitive, whole words, results feed to listbox, 
    • Ctrl+P, Ctrl+Enter, Ctrl+Shift+Enter – when inserting paths they should end with a space and be wrapped in quotes if they contain spaces.
    • Navigation history (Alt+Down).
    • Handling of symbolic links or file attributes (permissions, hidden).
    * F5/F6/F7 - refresh sibling panel  